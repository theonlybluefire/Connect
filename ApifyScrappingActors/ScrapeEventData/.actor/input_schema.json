{
    "title": "Scrape data from a web page",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "url": {
            "title": "URL of the page",
            "type": "string",
            "description": "The URL of website you want to get the data from.",
            "editor": "textfield",
            "prefill": ""
        },
         "region": {
            "title": "Region to use for the scrape",
            "type": "string",
            "description": "Region to use for the scrape, e.g., 'region-biberach', configured regions are available through the config tables.",
            "editor": "textfield",
            "prefill": ""
        }
    },
    "required": ["url", "region"]
}
